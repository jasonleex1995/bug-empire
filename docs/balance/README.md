# Balance artifacts (historical)

These tournament / duel dumps were generated **before** the locked roster
(ants 3 · beetles 2 · mantises 2) and utility-only defenses.

Strategy ids like `std2_mix_4L_turret` and unit names like 사슴벌레 / 무당벌레
refer to the old build. Re-run `npm run tournament` / `npm run duel` after a
balance pass to refresh numbers; until then treat this folder as archive.
